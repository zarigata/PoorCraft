# Java-Python bridge (must match Java dependency version in pom.xml)
py4j==0.10.9.7

# Image processing for procedural texture generation (Skin Generator mod)
Pillow>=10.0.0

# HTTP client for API calls to Gemini, OpenRouter, OpenAI (AI NPC mod)
requests>=2.31.0

# Numerical operations for texture generation algorithms
numpy>=1.24.0
